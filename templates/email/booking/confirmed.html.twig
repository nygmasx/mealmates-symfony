<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Réservation confirmée - Meal Mates</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
            background-color: #f8f9fa;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background: white;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header {
            background: linear-gradient(135deg, #4CAF50 0%, #388E3C 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 300;
        }
        .header .subtitle {
            margin: 10px 0 0 0;
            font-size: 16px;
            opacity: 0.9;
        }
        .success-badge {
            background: linear-gradient(135deg, #4CAF50 0%, #388E3C 100%);
            color: white;
            padding: 20px;
            border-radius: 12px;
            text-align: center;
            margin: 20px 0;
            font-size: 18px;
            font-weight: 600;
        }
        .content {
            padding: 30px 20px;
        }
        .greeting {
            font-size: 18px;
            color: #333;
            margin-bottom: 20px;
        }
        .booking-info {
            background: #e8f5e8;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
            border-left: 4px solid #4CAF50;
        }
        .booking-number {
            font-size: 14px;
            color: #666;
            margin-bottom: 10px;
        }
        .booking-date {
            font-size: 14px;
            color: #666;
            margin-bottom: 15px;
        }
        .status-badge {
            display: inline-block;
            background: #4CAF50;
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .seller-info {
            background: #e8f5e8;
            border-radius: 8px;
            padding: 20px;
            margin: 30px 0;
            border-left: 4px solid #4CAF50;
        }
        .seller-info h3 {
            margin: 0 0 10px 0;
            color: #2E7D32;
            font-size: 18px;
        }
        .seller-info p {
            margin: 5px 0;
            color: #388E3C;
        }
        .products-section {
            margin: 30px 0;
        }
        .section-title {
            font-size: 20px;
            color: #333;
            margin-bottom: 20px;
            border-bottom: 2px solid #e5e7eb;
            padding-bottom: 10px;
        }
        .product-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            border-bottom: 1px solid #e5e7eb;
        }
        .product-item:last-child {
            border-bottom: none;
        }
        .product-info h3 {
            margin: 0 0 5px 0;
            font-size: 16px;
            color: #333;
        }
        .product-description {
            font-size: 14px;
            color: #666;
            margin: 0;
        }
        .product-price {
            font-size: 18px;
            font-weight: 600;
            color: #4CAF50;
        }
        .total-section {
            background: #e8f5e8;
            border-radius: 8px;
            padding: 20px;
            margin: 30px 0;
            text-align: right;
            border-left: 4px solid #4CAF50;
        }
        .total-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }
        .total-label {
            font-size: 16px;
            color: #666;
        }
        .total-value {
            font-size: 16px;
            color: #333;
        }
        .total-final {
            border-top: 2px solid #4CAF50;
            padding-top: 15px;
            margin-top: 15px;
        }
        .total-final .total-label {
            font-size: 20px;
            font-weight: 600;
            color: #333;
        }
        .total-final .total-value {
            font-size: 24px;
            font-weight: 700;
            color: #4CAF50;
        }
        .next-steps {
            background: #e8f5e8;
            border-radius: 8px;
            padding: 20px;
            margin: 30px 0;
            border-left: 4px solid #4CAF50;
        }
        .next-steps h3 {
            margin: 0 0 15px 0;
            color: #2E7D32;
            font-size: 18px;
        }
        .next-steps ul {
            margin: 0;
            padding-left: 20px;
        }
        .next-steps li {
            margin-bottom: 8px;
            color: #388E3C;
        }
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #4CAF50 0%, #388E3C 100%);
            color: white;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            margin: 20px 0;
            text-align: center;
            transition: transform 0.2s;
        }
        .cta-button:hover {
            transform: translateY(-2px);
        }
        .warning-box {
            background: #fff3cd;
            border: 1px solid #ffc107;
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
        }
        .warning-box p {
            margin: 0;
            color: #856404;
            font-size: 14px;
        }
        .footer {
            background: #f8f9fa;
            padding: 30px 20px;
            text-align: center;
            border-top: 1px solid #e5e7eb;
        }
        .footer p {
            margin: 5px 0;
            color: #666;
            font-size: 14px;
        }
        .footer a {
            color: #4CAF50;
            text-decoration: none;
        }
        .social-links {
            margin: 20px 0;
        }
        .social-links a {
            display: inline-block;
            margin: 0 10px;
            color: #4CAF50;
            font-size: 18px;
            text-decoration: none;
        }
        @media (max-width: 600px) {
            .container {
                margin: 0;
                box-shadow: none;
            }
            .header, .content, .footer {
                padding: 20px 15px;
            }
            .product-item {
                flex-direction: column;
                align-items: flex-start;
                gap: 10px;
            }
            .total-row {
                flex-direction: column;
                gap: 5px;
            }
        }
    </style>
</head>
<body>
<div class="container">
    <!-- Header -->
    <div class="header">
        <h1>🍽️ Meal Mates</h1>
        <p class="subtitle">Excellente nouvelle !</p>
    </div>

    <!-- Content -->
    <div class="content">
        <!-- Success Badge -->
        <div class="success-badge">
            ✅ Votre réservation a été confirmée !
        </div>

        <div class="greeting">
            Bonjour {{ buyerName }},
        </div>

        <p>Fantastique ! <strong>{{ sellerName }}</strong> a confirmé votre réservation.
            Vous pouvez maintenant organiser la rencontre pour récupérer vos produits.</p>

        <!-- Booking Info -->
        <div class="booking-info">
            <div class="booking-number">
                <strong>Numéro de réservation :</strong> #{{ booking.id }}
            </div>
            <div class="booking-date">
                <strong>Confirmée le :</strong> {{ booking.confirmedAt|date('d/m/Y à H:i') }}
            </div>
            <span class="status-badge">✅ Confirmée</span>
        </div>

        <!-- Seller Info -->
        <div class="seller-info">
            <h3>👨‍🍳 Votre vendeur</h3>
            <p><strong>{{ sellerName }}</strong></p>
            {% set seller = booking.product.user %}
            {% if seller.email %}
                <p>📧 {{ seller.email }}</p>
            {% endif %}
            {% if seller.phone %}
                <p>📱 {{ seller.phone }}</p>
            {% endif %}
        </div>

        <!-- Products Section -->
        <div class="products-section">
            <h2 class="section-title">📦 Produits confirmés</h2>

            <div class="product-item">
                <div class="product-info">
                    <h3>{{ booking.product.title }}</h3>
                    <p class="product-description">Type: {{ booking.product.type }}</p>
                    {% if booking.product.expiresAt %}
                        <p class="product-description">
                            <strong>🗓️ Expire le :</strong> {{ booking.product.expiresAt|date('d/m/Y') }}
                        </p>
                    {% endif %}
                </div>
                <div class="product-price">
                    {% if booking.product.price == 0 %}
                        <span style="color: #4CAF50;">GRATUIT</span>
                    {% else %}
                        {{ booking.product.price|number_format(2, ',', ' ') }}€
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- Total Section -->
        <div class="total-section">
            <div class="total-row">
                <span class="total-label">Nombre d'articles :</span>
                <span class="total-value">1</span>
            </div>
            {% if booking.totalPrice > 0 %}
                <div class="total-row">
                    <span class="total-label">Sous-total :</span>
                    <span class="total-value">{{ booking.totalPrice|number_format(2, ',', ' ') }}€</span>
                </div>
            {% endif %}
            <div class="total-row total-final">
                <span class="total-label">Total à payer :</span>
                <span class="total-value">
                    {% if booking.totalPrice == 0 %}
                        GRATUIT 🎁
                    {% else %}
                        {{ booking.totalPrice|number_format(2, ',', ' ') }}€
                    {% endif %}
                </span>
            </div>
        </div>

        <!-- Next Steps -->
        <div class="next-steps">
            <h3>🚀 Prochaines étapes</h3>
            <ul>
                <li><strong>Contactez le vendeur :</strong> Organisez un rendez-vous pour la remise</li>
                <li><strong>Choisissez un lieu :</strong> Privilégiez un endroit public et pratique</li>
                <li><strong>Confirmez l'heure :</strong> Assurez-vous que vous êtes disponibles tous les deux</li>
                <li><strong>Préparez le paiement :</strong> Le règlement se fait en main propre</li>
                <li><strong>Vérifiez les produits :</strong> Contrôlez la qualité avant de payer</li>
            </ul>
        </div>

        {% if booking.totalPrice > 0 %}
            <div class="warning-box">
                <p>
                    💰 <strong>Rappel :</strong> Le paiement de {{ booking.totalPrice|number_format(2, ',', ' ') }}€
                    s'effectue directement auprès du vendeur lors de la remise des produits.
                </p>
            </div>
        {% endif %}

        <!-- CTA Button -->
        <div style="text-align: center;">
            <a href="https://mealmates.testingtest.fr/bookings/{{ booking.id }}" class="cta-button">
                💬 Contacter le vendeur
            </a>
        </div>

        <p style="margin-top: 30px; color: #666; text-align: center;">
            <strong>Félicitations pour votre réservation confirmée !</strong><br>
            N'hésitez pas à nous contacter si vous avez des questions.
        </p>
    </div>

    <div class="footer">
        <div class="social-links">
            <a href="#" title="Facebook">📘</a>
            <a href="#" title="Instagram">📷</a>
            <a href="#" title="Twitter">🐦</a>
        </div>

        <p><strong>Meal Mates</strong> - Ensemble contre le gaspillage alimentaire</p>
        <p>
            <a href="https://mealmates.testingtest.fr">Accueil</a> |
        </p>
        <p style="font-size: 12px; color: #999;">
            Cet email a été envoyé à {{ booking.user.email }}.
        </p>
    </div>
</div>
</body>
</html>
