<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Votre réservation Meal Mates</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
            background-color: #f8f9fa;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background: white;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .header {
            background: linear-gradient(135deg, #4CAF50 0%, #764ba2 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }

        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 300;
        }

        .header .subtitle {
            margin: 10px 0 0 0;
            font-size: 16px;
            opacity: 0.9;
        }

        .content {
            padding: 30px 20px;
        }

        .greeting {
            font-size: 18px;
            color: #333;
            margin-bottom: 20px;
        }

        .booking-info {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
            border-left: 4px solid #667eea;
        }

        .booking-number {
            font-size: 14px;
            color: #666;
            margin-bottom: 10px;
        }

        .booking-date {
            font-size: 14px;
            color: #666;
            margin-bottom: 15px;
        }

        .status-badge {
            display: inline-block;
            background: #fef3c7;
            color: #92400e;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .products-section {
            margin: 30px 0;
        }

        .section-title {
            font-size: 20px;
            color: #333;
            margin-bottom: 20px;
            border-bottom: 2px solid #e5e7eb;
            padding-bottom: 10px;
        }

        .product-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            border-bottom: 1px solid #e5e7eb;
        }

        .product-item:last-child {
            border-bottom: none;
        }

        .product-info h3 {
            margin: 0 0 5px 0;
            font-size: 16px;
            color: #333;
        }

        .product-description {
            font-size: 14px;
            color: #666;
            margin: 0;
        }

        .product-price {
            font-size: 18px;
            font-weight: 600;
            color: #10b981;
        }

        .total-section {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin: 30px 0;
            text-align: right;
        }

        .total-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .total-label {
            font-size: 16px;
            color: #666;
        }

        .total-value {
            font-size: 16px;
            color: #333;
        }

        .total-final {
            border-top: 2px solid #667eea;
            padding-top: 15px;
            margin-top: 15px;
        }

        .total-final .total-label {
            font-size: 20px;
            font-weight: 600;
            color: #333;
        }

        .total-final .total-value {
            font-size: 24px;
            font-weight: 700;
            color: #667eea;
        }

        .seller-info {
            background: #ecfdf5;
            border-radius: 8px;
            padding: 20px;
            margin: 30px 0;
            border-left: 4px solid #10b981;
        }

        .seller-info h3 {
            margin: 0 0 10px 0;
            color: #065f46;
            font-size: 18px;
        }

        .seller-info p {
            margin: 5px 0;
            color: #047857;
        }

        .next-steps {
            background: #fef7ff;
            border-radius: 8px;
            padding: 20px;
            margin: 30px 0;
            border-left: 4px solid #a855f7;
        }

        .next-steps h3 {
            margin: 0 0 15px 0;
            color: #7c2d12;
            font-size: 18px;
        }

        .next-steps ul {
            margin: 0;
            padding-left: 20px;
        }

        .next-steps li {
            margin-bottom: 8px;
            color: #86198f;
        }

        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            margin: 20px 0;
            text-align: center;
            transition: transform 0.2s;
        }

        .cta-button:hover {
            transform: translateY(-2px);
        }

        .footer {
            background: #f8f9fa;
            padding: 30px 20px;
            text-align: center;
            border-top: 1px solid #e5e7eb;
        }

        .footer p {
            margin: 5px 0;
            color: #666;
            font-size: 14px;
        }

        .footer a {
            color: #667eea;
            text-decoration: none;
        }

        .social-links {
            margin: 20px 0;
        }

        .social-links a {
            display: inline-block;
            margin: 0 10px;
            color: #667eea;
            font-size: 18px;
            text-decoration: none;
        }

        .warning-box {
            background: #fef2f2;
            border: 1px solid #fecaca;
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
        }

        .warning-box p {
            margin: 0;
            color: #b91c1c;
            font-size: 14px;
        }

        @media (max-width: 600px) {
            .container {
                margin: 0;
                box-shadow: none;
            }

            .header, .content, .footer {
                padding: 20px 15px;
            }

            .product-item {
                flex-direction: column;
                align-items: flex-start;
                gap: 10px;
            }

            .total-row {
                flex-direction: column;
                gap: 5px;
            }
        }
    </style>
</head>
<body>
<div class="container">
    <div class="header">
        <h1>🍽️ Meal Mates</h1>
        <p class="subtitle">Votre réservation a été créée avec succès</p>
    </div>

    <div class="content">
        <div class="greeting">
            Bonjour {{ booking.user.firstName }},
        </div>

        <p>Merci pour votre réservation ! Voici le récapitulatif de votre commande :</p>

        <div class="booking-info">
            <div class="booking-number">
                <strong>Numéro de réservation :</strong> #{{ booking.id }}
            </div>
            <div class="booking-date">
                <strong>Date de création :</strong> {{ booking.createdAt|date('d/m/Y à H:i') }}
            </div>
            <span class="status-badge">⏳ En attente de confirmation</span>
        </div>

        <div class="products-section">
            <h2 class="section-title">📦 Produits réservés</h2>

            <div class="product-item">
                <div class="product-info">
                    <h3>{{ booking.product.title }}</h3>
                    {% if booking.product.expiresAt %}
                        <p class="product-description">
                            <strong>🗓️ Expire le :</strong> {{ booking.product.expiresAt|date('d/m/Y') }}
                        </p>
                    {% endif %}
                </div>
                <div class="product-price">
                    {% if booking.product.price == 0 %}
                        <span style="color: #10b981;">GRATUIT</span>
                    {% else %}
                        {{ booking.product.price|number_format(2, ',', ' ') }}€
                    {% endif %}
                </div>
            </div>
        </div>

        <div class="total-section">
            <div class="total-row">
                <span class="total-label">Nombre d'articles :</span>
                <span class="total-value">1</span>
            </div>
            {% if booking.totalPrice > 0 %}
                <div class="total-row">
                    <span class="total-label">Sous-total :</span>
                    <span class="total-value">{{ booking.totalPrice|number_format(2, ',', ' ') }}€</span>
                </div>
            {% endif %}
            <div class="total-row total-final">
                <span class="total-label">Total à payer :</span>
                <span class="total-value">
                        {% if booking.totalPrice == 0 %}
                            GRATUIT 🎁
                        {% else %}
                            {{ booking.totalPrice|number_format(2, ',', ' ') }}€
                        {% endif %}
                    </span>
            </div>
        </div>

        {% set seller = booking.product.user %}
        <div class="seller-info">
            <h3>👨‍🍳 Votre vendeur</h3>
            <p><strong>{{ seller.firstName }}</strong></p>
            {% if seller.email %}
                <p>📧 {{ seller.email }}</p>
            {% endif %}
        </div>

        <div class="next-steps">
            <h3>🚀 Prochaines étapes</h3>
            <ul>
                <li><strong>Confirmation :</strong> Le vendeur a 24h pour confirmer votre réservation</li>
                <li><strong>Contact :</strong> Une fois confirmée, vous pourrez organiser la rencontre</li>
                <li><strong>Paiement :</strong> Le règlement se fait directement lors de la remise</li>
                <li><strong>Récupération :</strong> Coordonnez-vous pour le lieu et l'heure de rendez-vous</li>
            </ul>
        </div>

        {% if booking.totalPrice > 0 %}
            <div class="warning-box">
                <p>
                    ⚠️ <strong>Important :</strong> Le paiement de {{ booking.totalPrice|number_format(2, ',', ' ') }}€
                    s'effectue directement auprès du vendeur lors de la remise des produits.
                </p>
            </div>
        {% endif %}

        <div style="text-align: center;">
            <a href="https://mealmates.testingtest.fr/messages/{{ booking.id }}" class="cta-button">
                📱 Suivre ma réservation
            </a>
        </div>

        <p style="margin-top: 30px; color: #666;">
            Vous recevrez une notification dès que le vendeur aura répondu à votre demande.
            En cas de question, n'hésitez pas à nous contacter.
        </p>
    </div>

    <div class="footer">
        <p><strong>Meal Mates</strong> - Ensemble contre le gaspillage alimentaire</p>
        <p>
            <a href="https://mealmates.testingtest.fr">Accueil</a>
        </p>
        <p style="font-size: 12px; color: #999;">
            Cet email a été envoyé à {{ booking.user.email }}.<br>
            Si vous ne souhaitez plus recevoir ces emails,
        </p>
    </div>
</div>
</body>
</html>
